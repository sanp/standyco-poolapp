defaults: &defaults

  debug: True

  databases:
    default:
      ENGINE: django.db.backends.postgresql_psycopg2
      NAME: poolapp_django
      USER: poolapp
      PASSWORD: standyco # Move this into separete yaml file
      HOST: localhost
      PORT: 5432

development:
  <<: *defaults
  
staging:
  <<: *defaults

production:
  <<: *defaults

  debug: True

  databases:
    default:
      ENGINE: django.db.backends.postgresql_psycopg2
